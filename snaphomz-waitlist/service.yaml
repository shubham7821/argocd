apiVersion: v1
kind: Service
metadata:
  name: snaphomz
spec:
  selector:
    app: snaphomz
  type: NodePort
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 32083  # 32083 is arbitrary; just needs to be > 30000 and < 32767
